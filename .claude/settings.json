{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "//": "项目级权限白名单（python/ 为 git 仓库根）。",
  "permissions": {
    "allow": [
      "uv run *",
      "uv pip install",
      "uv pip sync",
      "uv venv",
      "git *"
    ],
    "deny": []
  }
}
